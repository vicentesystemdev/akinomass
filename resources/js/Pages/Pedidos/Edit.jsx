import { useForm } from '@inertiajs/react';
export default function Edit({ pedido, estadoBorrador }) { const { data, setData, put } = useForm({ ...pedido, detalles: pedido.detalles ?? [] }); const submit=e=>{e.preventDefault(); put(route('pedidos.update', pedido.cod_pedido));}; return <form onSubmit={submit}><h1>Editar Pedido</h1>{pedido.estado_ped!==estadoBorrador && <p>Pedido no editable</p>}<textarea value={data.observacion_ped ?? ''} onChange={e=>setData('observacion_ped', e.target.value)} /><button type='submit' disabled={pedido.estado_ped!==estadoBorrador}>Actualizar</button></form>; }
