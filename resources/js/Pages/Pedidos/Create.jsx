import { useForm } from '@inertiajs/react';
export default function Create({ clientes, canales, tiposFlujo, productos }) { const { data, setData, post } = useForm({ cod_cliente:'', cod_canal_venta:'', cod_tipo_flujo_comercial:'', descuento_ped:0, observacion_ped:'', detalles:[{cod_producto:'',cantidad_det:1,precio_unitario_det:0}] }); const submit=e=>{e.preventDefault(); post(route('pedidos.store'));}; return <form onSubmit={submit}><h1>Crear Pedido</h1><select onChange={e=>setData('cod_cliente',e.target.value)}>{clientes.map(c=><option key={c.cod_cliente} value={c.cod_cliente}>{c.nombre_cli}</option>)}</select><button type='submit'>Guardar</button></form>; }
