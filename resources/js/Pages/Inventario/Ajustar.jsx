import { Head, useForm } from '@inertiajs/react';
export default function Ajustar({ productos }) { const { data, setData, post } = useForm({ cod_producto: '', stock_nuevo_mov: 0, motivo_mov: '', observacion_mov: '' }); return <><Head title="Ajuste" /><div className="p-6"><h1>Ajustar inventario</h1><form onSubmit={(e)=>{e.preventDefault();post(route('inventario.ajuste'));}}><select value={data.cod_producto} onChange={(e)=>setData('cod_producto', e.target.value)}>{productos.map((p)=><option key={p.cod_producto} value={p.cod_producto}>{p.nombre_pro}</option>)}</select><input type="number" value={data.stock_nuevo_mov} onChange={(e)=>setData('stock_nuevo_mov', e.target.value)} /><input value={data.motivo_mov} onChange={(e)=>setData('motivo_mov', e.target.value)} /><button type="submit">Guardar</button></form></div></>; }
