import { useForm } from '@inertiajs/react'; export default function Create({canales,estados}){const {data,setData,post}=useForm({titulo_ses:'',fecha_inicio_ses:'',estado_ses:estados[0],cod_canal_venta:canales[0]?.cod_canal_venta});return <form onSubmit={e=>{e.preventDefault();post(route('live-sales.store'));}}><input value={data.titulo_ses} onChange={e=>setData('titulo_ses',e.target.value)} placeholder='Título'/><input type='datetime-local' onChange={e=>setData('fecha_inicio_ses',e.target.value)}/><button>Guardar</button></form>}
